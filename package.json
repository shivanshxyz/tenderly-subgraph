{
  "name": "testing",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy tenderly-subgraph --ipfs http://localhost:5001 --node http://localhost:8020",
    "create-local": "graph create --node http://localhost:8020/ testing",
    "remove-local": "graph remove --node http://localhost:8020/ testing",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 testing",
    "test": "graph test",
    "create": "graph create tenderly-subgraph --node http://localhost:8020"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.72.2",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
